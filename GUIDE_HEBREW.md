# 🖨️ המביא לדפוס — הדרכה מלאה מההתחלה ועד הסוף

---

## 📋 שלב 1: התקנת Python

אם עדיין לא מותקן:
1. לך לאתר: https://www.python.org/downloads/
2. לחץ על "Download Python 3.x.x"
3. **חשוב מאוד:** בחלון ההתקנה, סמן ✓ את **"Add Python to PATH"**
4. לחץ "Install Now"

בדיקה: פתח cmd והקלד:
```
python --version
```
אמור להופיע: `Python 3.x.x`

---

## 📋 שלב 2: התקנת ספריות Python

בcmd, עבור לתיקיית virtual_printer והרץ:
```
pip install PyQt6 PyPDF2
```

---

## 📋 שלב 3: התקנת המדפסת הווירטואלית

**חשוב:** הכלי חייב לפעול ברקע כדי שהדפסה תיפתח אוטומטית.

### א. התקן את המדפסת:
לחץ פעמיים על **INSTALL_PRINTER.bat** ← אשר "הפעל כמנהל"

צפה לפלט:
```
>>> Step 1: Remove old printer...
>>> Step 2: Ensure port directory...
>>> Step 3: Remove old port...
>>> Step 4: Register port in registry...
>>> Step 5: Restart Print Spooler...
>>> Step 6: Verify driver...
>>> Step 7: Add printer...
>>> Step 8: Verify...
=== SUCCESS: 'המביא לדפוס' installed! ===
```

### ב. הפעל את הכלי:
```
python main.py
```

### ג. הגדר הפעלה אוטומטית (חשוב!):
בתוך הכלי → ⚙️ הגדרות → כללי → סמן ✓ "הפעל בעת אתחול Windows"

**מדוע זה חשוב?** הכלי חייב לפעול ברקע כדי שבעת שליחת הדפסה מ-Word/Chrome, הקובץ ייפתח אוטומטית בכלי. ללא הפעלה ברקע — תצטרך לפתוח את הכלי ידנית לפני כל הדפסה.

---

## 🖨️ שלב 4: שימוש יומיומי — הדפסה מ-Word/Chrome

1. **פתח את הכלי** (או שהוא פועל ברקע אם הגדרת הפעלה אוטומטית)
2. בכל תוכנה (Word, Chrome, Adobe Reader...) לחץ **הדפס** (Ctrl+P)
3. ברשימת המדפסות בחר: **המביא לדפוס**
4. לחץ **הדפס** — חלון הכלי יקפוץ קדימה עם המסמך טעון
5. בחר **אפשרות הדפסה** (רגיל, חוברת, דו-צדדי וכו')
6. לחץ **הדפס עכשיו** — המסמך ישלח למדפסת האמיתית

---

## 📂 שלב 5: הדפסה ישירה מהכלי (ללא Word/Chrome)

1. פתח את הכלי
2. **גרור קובץ** לחלון, או לחץ **בחר קובץ**
3. בחר אפשרות הדפסה ולחץ **הדפס עכשיו**

---

## ⚙️ הגדרות מפתח

### כניסה להגדרות:
לחץ ⚙️ בחלון הראשי (סיסמת ברירת מחדל: **1234**)
או לחץ F8 בכל מקום בחלון

### לשוניות ההגדרות:

**אפשרויות** — ניהול אפשרויות הדפסה מהירה:
- סמן/בטל אפשרויות שיוצגו בחלון הראשי
- לחץ פעמיים לעריכת אפשרות (שם, תמונה, מתגים, מדפסת ייעודית)

**מדפסות** — הגדר מדפסת ברירת מחדל / הצג/הסתר בחירת מדפסת

**אבטחה** — שינוי סיסמת ההגדרות

**כללי** — תאריך/שעה, קיצורי מקשים, הפעלה עם Windows

---

## ❓ פתרון בעיות

**המדפסת לא מופיעה ב-Word/Chrome:**
→ הרץ שוב את INSTALL_PRINTER.bat כמנהל מערכת
→ סגור ופתח מחדש את Word/Chrome

**שלחתי הדפסה למדפסת אבל הכלי לא נפתח:**
→ הכלי לא פועל ברקע! פתח אותו ידנית, או הפעל "הפעלה עם Windows" בהגדרות

**שכחתי סיסמת הגדרות:**
→ מחק את הקובץ: `C:\Users\[שמך]\AppData\Roaming\HaMeviBLaDfus\config.json`
→ הסיסמה תתאפס ל-1234

**הסרת המדפסת:**
→ לחץ פעמיים על UNINSTALL_PRINTER.bat

---

## 📁 קבצים בתיקייה

| קובץ | תפקיד |
|------|-------|
| `main.py` | הכלי הראשי |
| `INSTALL_PRINTER.bat` | התקנת המדפסת הווירטואלית |
| `UNINSTALL_PRINTER.bat` | הסרת המדפסת |
| `install_printer.py` | סקריפט ההתקנה (נקרא מה-BAT) |
| `settings_manager.py` | ניהול הגדרות |
| `print_engine.py` | מנוע הדפסה |
| `ui_components.py` | רכיבי ממשק |
| `hebrew_date.py` | חישוב תאריך עברי |

---

גרסה 1.1 | Windows 10/11 | Python 3.10+
